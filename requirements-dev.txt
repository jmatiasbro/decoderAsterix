# Herramientas de verificación (tests y cobertura). Instalar con:
#   pip install -r requirements-dev.txt
# Ver 09_SVP.md §4 (ejecución de la suite) y el workflow .github/workflows/ci.yml.
pytest>=8.0
pytest-cov>=5.0
pytest-html>=4.0
