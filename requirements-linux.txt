# Dependencias para correr la app en Linux (mismo código que en Windows).
# El requirements.txt original está incompleto; esta lista refleja TODOS los
# paquetes de terceros que importa la aplicación GUI (relevado del código).
# Versiones = las probadas en Windows (floor con >=, para permitir wheels de Linux).
#
# Instalación:
#   python3 -m venv .venv-linux
#   source .venv-linux/bin/activate
#   pip install -r requirements-linux.txt
#
# Ver docs/INSTALL_LINUX.md para dependencias de sistema (Qt) y notas.

PyQt6>=6.11.0
numpy>=2.4.4
matplotlib>=3.10.9
pyproj>=3.7.2
dpkt>=1.9.8
duckdb>=1.5.3
pyqtgraph>=0.14.0
qtawesome>=1.4.2
fpdf2>=2.8.7
pygeomag>=1.1.0
simplekml>=1.3.6

# Opcional: solo si se usa el servidor web (player/web_server.py), no requerido
# por la consola PPI ni la suite técnica.
# flask>=3.0
